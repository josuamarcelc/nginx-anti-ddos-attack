# =============================================================================
# DDoS Whitelist — IPs that should NEVER be auto-blocked
# =============================================================================
# One IP per line. CIDR notation supported.
# Example:
#   203.0.113.10
#   198.51.100.0/24
# =============================================================================

# Your server's own public IP (IMPORTANT — add this)
# x.x.x.x

# Monitoring / health check IPs
# x.x.x.x

# Office / VPN IPs
# x.x.x.x
